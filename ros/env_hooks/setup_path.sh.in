#!/usr/bin/env sh
# generated from $(find source_library_paths)/env-hooks/setup.sh.in

if [ "@DEVELSPACE@" = "True" ]; then
  DIR_PATH=@DIR_PATH_DEVEL@
else
  DIR_PATH=@DIR_PATH_INSTALL@
fi
if ! echo "$PATH" | grep -q "$DIR_PATH"; then
	export PATH=$DIR_PATH:$PATH
fi
