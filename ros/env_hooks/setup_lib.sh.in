#!/usr/bin/env sh
# generated from $(find source_library_paths)/env-hooks/setup.sh.in

if ! echo "$LD_LIBRARY_PATH" | grep -q "@LIBRARY_PATH@"; then
	export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:@LIBRARY_PATH@
fi