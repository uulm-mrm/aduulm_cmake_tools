#!/usr/bin/env sh
# generated from $(find source_library_paths)/env-hooks/setup.zsh.in

# source setup.sh from same directory as this file
_LIB_SETUP_DIR=$(builtin cd -q "`dirname "$0"`" > /dev/null && pwd)
emulate -R zsh -c 'source "$_LIB_SETUP_DIR/setup_@DIR_NAME@.sh"'
